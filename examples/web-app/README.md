# Web App Example

General-purpose holding folder for web-app example material — screenshots,
recordings, or reference files. Exact use is still open; refine this note
once that's settled. Not wired into the app build.

Files placed here are gitignored (see root `.gitignore`) so they stay
local-only. Only this README and the folder structure itself are tracked
in git.
