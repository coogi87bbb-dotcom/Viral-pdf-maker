# Brand Assets

Raw brand photography and video files live here — not wired into the app
build (nothing under `src/` imports from this folder).

- `photos/` — brand photos
- `videos/` — brand videos

These files are gitignored (see root `.gitignore`) so they stay local-only
and don't bloat the repo. Only this README and the folder structure itself
are tracked in git.
